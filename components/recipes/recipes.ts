import { css } from "@emotion/css";
import { display } from "@mui/system";

export const recipesCard = css({
  margin: "20px",
});

export const recipeImg = css({
  display: "flex",
  justifyContent: "center",
});

export const recipesImage = css({
  display: "flex",
  justifyContent: "center",
  maxWidth: "230px",
  minHeight: "250px",
});

export const recipesText = css({
  display: "flex",
  justifyContent: "center",
});

export const recipe = css({
  marginTop: "50px",
  // display: "flex",
  justifyContent: "center",
});
