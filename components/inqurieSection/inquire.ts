import { css } from "@emotion/css";

export const reachout = css({
  display: "flex",
});

export const block = css({
  display: "block",
});

export const appimage = css({
  display: "block",
  maxWidth: "500px",
  maxHeight: "50px",
  margin: "2rem 1rem",
});

export const reachouttext = css({
  marginTop: "70px",
});
