import { css } from "@emotion/css";

export const youtube = css({
  marginTop: "50px",
});

export const video = css({
  maxWidth: "500px",
});

export const youtubeSection = css({
  display: "flex",
});

export const blockSection = css({
  display: "block",
});

export const aboutVneed = css({
  padding: "50px",
});

export const yvideo = css({
  display: "flex",
  justifyContent: "center",
});
